Add these audio assets for the TikTok Initiate Launch sequence:

boot.mp3     ~0.4s, boot/startup tone
confirm.mp3  ~0.25-0.4s, soft confirmation tone

Keep each file <100kb. Use subtle, luxury tones (no bouncy/playful).
The app works without these; playback fails silently if missing.
