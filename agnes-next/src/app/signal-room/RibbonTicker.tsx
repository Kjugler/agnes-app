'use client';

import SiteRibbonTicker from '@/components/SiteRibbonTicker';

/** Signal Room: canonical ribbon only (no extra segments). */
export default function RibbonTicker() {
  return <SiteRibbonTicker />;
}
