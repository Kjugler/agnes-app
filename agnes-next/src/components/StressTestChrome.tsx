'use client';

/**
 * Layout wrapper: global stress-test banner removed (simulation copy lives on buy/checkout only).
 */
export default function StressTestChrome({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
