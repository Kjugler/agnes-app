# Optional local dev: the-agnes-protocol.epub when EBOOK_FILE_URL is unset (see env.cjs).
# Production: set EBOOK_FILE_URL to the Vercel Blob URL; DeepQuill streams via fetch.
# *.epub / *.pdf / *.mobi under this folder are gitignored.
